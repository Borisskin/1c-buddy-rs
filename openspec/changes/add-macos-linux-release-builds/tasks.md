## 1. Проверка договора до реализации

- [x] 1.1 Добавить `tests/release_workflow_contract.rs` с отдельными проверками целей Linux и macOS, имён архивов, независимой матрицы и зависимости публикации от обоих заданий кандидатов
- [x] 1.2 Запустить новую проверку на исходном Windows-сценарии и зафиксировать ожидаемое падение из-за отсутствующих целей Unix

## 2. Проверки Windows, Linux и macOS

- [x] 2.1 Переименовать сценарий и группу одновременного выполнения, убрать глобальный `RUSTFLAGS` и задать статическую MSVC непосредственно в заданиях Windows
- [x] 2.2 Добавить `unix-build-and-check` с независимой матрицей `x86_64-unknown-linux-gnu` и `aarch64-apple-darwin`, закреплёнными средами, Rust 1.97.1, кэшем и `cargo test --locked --all-features`
- [x] 2.3 Убедиться статической проверкой договора, что `RUSTFLAGS` ограничен заданиями Windows, прежняя проверка Windows сохранена, а обе новые цели обязательны для изменения

## 3. Кандидаты выпуска Unix

- [x] 3.1 Добавить матричное задание `unix-release-candidate`, которое собирает Linux и macOS из той же редакции после успешных проверок
- [x] 3.2 Записать для каждой цели среду, Rust, Cargo, версию, метку, редакцию, имя архива и SHA-256 исполняемого файла
- [x] 3.3 Упаковать ровно пять обязательных файлов в `onec-buddy-mcp-linux-x86_64.tar.gz` и `onec-buddy-mcp-macos-aarch64.tar.gz`, проверить состав и SHA-256 вложенного файла
- [x] 3.4 Загрузить каждый кандидат и его целевые сведения отдельным артефактом GitHub Actions без коллизий имён

## 4. Единая публикация

- [x] 4.1 Расширить зависимости `publish-release` и скачать Windows, Linux и macOS в отдельные каталоги
- [x] 4.2 Повторно проверить у трёх кандидатов версию, метку, редакцию, цель, имя архива, SHA-256 архива и SHA-256 вложенного исполняемого файла
- [x] 4.3 Создать общий `SHA256SUMS.txt` из повторно вычисленных сумм трёх архивов
- [x] 4.4 Публиковать одной командой ровно три проверенных архива и общий перечень контрольных сумм, сохранив запрет повторного использования версии

## 5. Документация

- [x] 5.1 Обновить `README.md`: три поддерживаемые поставки, выбор архива и краткий запуск для каждой системы
- [x] 5.2 Обновить `README_FULL.md`: состав архивов, проверка SHA-256, распаковка, право на выполнение и абсолютные пути Codex, Cursor и Claude для Linux и macOS
- [x] 5.3 Явно описать границы: macOS только Apple Silicon, Linux x86-64 GNU, подпись и нотариальное заверение не входят в изменение

## 6. Итоговая проверка

- [x] 6.1 Выполнить `openspec validate add-macos-linux-release-builds --strict`
- [x] 6.2 Выполнить проверку договора сборочного сценария и убедиться, что она проходит после реализации
- [ ] 6.3 Проверить Windows, Linux и macOS в GitHub, вручную собрать кандидаты рабочей ветви без выпуска и скачать двоичные файлы
- [x] 6.4 Проверить итоговый `git diff`, отсутствие случайных изменений в `vendor` и согласованность артефактов OpenSpec
